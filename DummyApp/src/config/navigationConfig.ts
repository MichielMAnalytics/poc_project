/**
 * Navigation Configuration
 * Shared between native app and web preview
 * Separated from AppNavigator to avoid importing native-only navigation libraries
 */

export const INITIAL_PARAMS = {
  Home: {
    username: 'Jane Smith',
  },
};
